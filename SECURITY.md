# セキュリティ方針

最終更新: 2026-06-03

## 対象

この文書は、ソフトテニス試合ノートの公開Webアプリと、このリポジトリ内のコード、ドキュメント、テストデータを対象にします。

## 基本方針

- 入力データは端末内保存を基本とする
- 外部APIへ試合データを自動送信しない
- 個人名を含む実データはGit管理しない
- 公開リポジトリには匿名化済みテストデータだけを置く
- CSV出力では数式注入対策を行う
- HTML表示ではユーザー入力をエスケープする
- Content Security Policyで外部スクリプト読み込みを制限する

## 脆弱性を見つけた場合

現時点では個人開発・テスト公開のため、GitHub Issuesで詳細な個人情報や悪用手順を公開しないでください。

連絡時は、以下だけを簡潔に共有してください。

- どの画面で起きたか
- どのブラウザ、端末で起きたか
- 期待した動作と実際の動作
- 個人名、学校名、大会名を含まない再現手順

## 公開してはいけない情報

- 実名入りCSV
- 学校名、選手名、保護者名を含む検証データ
- APIキー、トークン、Cookie、認証情報
- 個人端末の絶対パス、作業フォルダ名、端末固有の一時フォルダ
- 非公開の起動URL、検証URL、作業用URL
- 個人端末のバックアップファイル
- 引き継ぎ用データの実ファイル

## 公開前チェック

公開前に次を実行します。

```bash
npm run check:public-info
```

このチェックは、公開文書やコードに個人環境由来の情報が混ざっていないかを確認するためのものです。

## 依存関係

本アプリは静的HTML/CSS/JavaScriptで動作します。開発用の依存関係は主にPlaywrightです。

最終リリース前に `npm audit --audit-level=moderate` を実行し、2026-06-03時点で `found 0 vulnerabilities` を確認しています。

## 免責

このアプリは試合記録と振り返りを補助するものです。完全なセキュリティ、データ保持、分析精度、指導効果を保証するものではありません。
